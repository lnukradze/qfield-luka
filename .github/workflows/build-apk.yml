name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: opengisch/qfield-sdk:20210413

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Link source to expected path
        run: |
          rm -rf /usr/src/qfield
          ln -s "$GITHUB_WORKSPACE" /usr/src/qfield
          echo "Source linked: $(ls /usr/src/qfield | head -5)"

      - name: Build APK
        env:
          APK_VERSION_CODE: "10906030"
          APP_VERSION_STR: "v1.9.6-LukaNukradze"
          APP_VERSION: "v1.9.6"
          ARCH: x86_64
          BUILD_FOLDER: build-x86_64
        run: |
          chmod +x /usr/src/qfield/scripts/docker-build.sh
          bash /usr/src/qfield/scripts/docker-build.sh

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: QField-LukaNukradze-x86_64.apk
          path: build-x86_64/android-build/build/outputs/apk/**/*.apk
          if-no-files-found: warn

      - name: Upload build logs on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: build-logs
          path: |
            build-x86_64/CMakeFiles/CMakeError.log
            build-x86_64/CMakeFiles/CMakeOutput.log
            build-x86_64/**/*.log
          if-no-files-found: ignore
